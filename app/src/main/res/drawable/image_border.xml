<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Couleur de fond de l'image (ici blanche) -->
    <stroke
        android:color="@android:color/black"
    android:width="4dp"/>
    <corners android:radius="8dp"/>
</shape>
